import {
    Input, InputDateTime, Button, ButtonCard
} from './form';
import {
    Logo, Flower, ThankYou,
    CustomView, Navbar, BackgroundWrapper,
    ParallaxScrollView
} from './partials';
import {
    Heading, TextFont, TextStatus, TextInfo, TotalPrice
} from './text';

export {
    Input, InputDateTime,
    Button, ButtonCard,
    Heading, TextFont, TextStatus, TextInfo, TotalPrice,
    Logo, Flower, ThankYou,
    CustomView, Navbar, BackgroundWrapper,
    ParallaxScrollView
}

export default {
    Input,
    Button, ButtonCard,
    Heading, TextFont, TextStatus, TextInfo, TotalPrice,
    Logo, Flower, ThankYou,
    CustomView, Navbar, BackgroundWrapper,
    ParallaxScrollView
}